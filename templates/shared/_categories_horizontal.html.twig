{% if activeOrg %}
<nav class="navbar navbar-expand bg-body-tertiary shadow">
  <div class="container-fluid">
    <div class="ms-3 me-3 btn-group ff-bebas align-items-start overflow-x-auto">
      <div class="mt-3" {{ stimulus_controller('modal-form', {formUrl: path('app_category_new', {'orgId': activeOrgId})}) }} data-action="hidden.bs.modal->modal-form#modalHidden">
        <div class="my-2"> <button class="btn btn-outline-danger btn-sm" data-action="modal-form#openModal" style="min-width: 150px;">NEW CATEGORY</button>
        {{ include("shared/_modal.html.twig", {modalTitle: 'Create a new Category'}) }}
      </div>
      {% for cat in activeOrg.categories %}
        {% set activeClass = cat.id == activeCatId ? "active" : "" %}
        <a class="btn btn-outline-success btn-sm ms-1 w-150 {{ activeClass }}" href="{{ path('app_home', {'orgId': activeOrgId, 'catId': cat.id}) }}">
          {{ cat.categoryName }}
        </a>
      {% endfor %}
    </div>
  </div>
</nav>
{% endif %}