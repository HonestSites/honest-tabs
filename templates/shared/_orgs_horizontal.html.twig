<nav class="navbar navbar-expand bg-body-tertiary shadow">
  <div class="container-fluid">
    <div class="ms-3 me-3 btn-group ff-bebas align-items-start overflow-x-auto">
      <div {{ stimulus_controller('modal-form', {formUrl: path('app_organization_new')}) }} data-action="hidden.bs.modal->modal-form#modalHidden">
        <button class="btn btn-outline-danger btn-sm" data-action="modal-form#openModal" style="min-width: 150px;">NEW ORGANIZATION</button>
        {{ include("shared/_modal.html.twig", {modalTitle: 'Create a new Organization'}) }}
      </div>
      {% for org in organizations %}
        {% set activeClass = org.id == activeOrgId ? "active" : "" %}
        <a class="btn btn-outline-success btn-sm ms-1 w-150 {{ activeClass }}" href="{{ path('app_home', {'orgId': org.id}) }}">
          {{ org.organizationName }}
        </a>
      {% endfor %}
    </div>
  </div>
</nav>