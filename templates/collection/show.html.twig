{% extends 'base.html.twig' %}
{% import getPath(_self) ~ "/_variables.twig" as globalData %}

{% block title %}{{ globalData.areaName }} - {{ linkCollections.collectionName }}{% endblock %}

{% block body %}
  {% set recordId = linkCollections.id %}
  <div class="container-xl mt-5">
    <div class="row">
      <div class="col pt-5">
        <h1 class="h3">{{ linkCollections.collectionName }}</h1>

        <table class="table">
          <tbody>
          <tr>
            <th>Category Name</th>
            <td>{{ linkCollections.collectionName }}</td>
          </tr>
          <tr>
            <th>Active</th>
            <td>{{ linkCollections.active ? 'Yes' : 'No' }}</td>
          </tr>
          <tr>
            <th>Allow Sharing</th>
            <td>{{ linkCollections.allowSharing ? 'Yes' : 'No' }}</td>
          </tr>
          </tbody>
        </table>

        {% include 'common/_back_link.html.twig' with {'pathName': globalData.backPathName, 'areaName': globalData.areaName } %}
        {% include 'common/_edit_link.html.twig' with {'pathName': globalData.editPathName, 'recordId':recordId, 'areaName': globalData.areaName } %}
        {% include 'common/_delete_link.html.twig' with {'pathName': globalData.deletePathName, 'recordId':recordId, 'areaName': globalData.areaName } %}
      </div>
    </div>
  </div>
{% endblock %}
